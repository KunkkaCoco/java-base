1Ã°ÅÝ

package com.cn.sort;

public class BubbleSort {

	public void sort(int[] arr){
		if(null != arr ){
			for (int i = 0; i < arr.length; i++) {
				for (int j = 0; j < arr.length-1-i; j++) {
					if(arr[j]>arr[j+1]){
						int t = arr[j];
						arr[j] = arr[j+1];
						arr[j+1] = t;
					}
				}
			}
		}
	}
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		BubbleSort bub = new BubbleSort();
		int[] array  = new int[]{2,4,5,3,7,6,1};
		bub.sort(array);
		for (int i = 0; i < array.length; i++) {
			System.out.println(array[i]);
		}
		

	}

}
2/Ñ¡ÔñÅÅÐò
package com.cn.sort;

public class SelectSort {
  
	private int min ;
	private int out;
	private int in;
	public void sort(int[] arr){
		for ( out = 0; out < arr.length-1; out++) {
			min = out;
			for ( in = out+1; in < arr.length; in++) {
				if(arr[in]<arr[min]){
					min = in;
				}
			}
					int t = arr[out];
					arr[out] = arr[min];
					arr[min] = t;
			
		}
		
	}
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		SelectSort sel = new SelectSort();
		 int[] array  = new int[]{8,2,4,5,3,7,6,1};
		sel.sort(array);
		for (int i = 0; i < array.length; i++) {
			System.out.println(array[i]);
		}
	}

}

